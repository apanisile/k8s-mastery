pipeline {
     environment {
        registryCredential = 'dockerHub'
        imagename = 'apanisile/k8s-mastery'
        dockerImage = ''
    }
    agent any
    stages {
        stage('Build') { 
            steps {
                sh 'cd frontend && npm start' 
            }
        }
         stage('Test') { 
            steps {
                sh 'npm test' 
            }
        }
    }
}